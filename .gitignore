# Everything that is yours. git never tracks it, so `git pull` never touches it.
# Your bots' settings hold tokens and exchange keys: never commit them.
*.env
!bot.example.env
compose.override.yml
# Passed between setup.sh / setup.cmd and the setup while it runs.
.setup-*
logs/
